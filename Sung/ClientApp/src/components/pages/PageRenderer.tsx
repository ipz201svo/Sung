import { defaultPagesType, defaultPages } from './defaultPages';

type PageRendererProps = {
  page: defaultPagesType;
}

// const PageRenderer = ({ page } : PageRendererProps) : JSX.Element => {
//   return defaultPages[page];
// };